{% macro inline_form(block, form) %}
    {% set form_action = path('netgen_block_manager_api_v1_block_form', {'blockId': block.id, 'formName': 'inline'}) %}

    {{ form_start(form, {'action': form_action, 'attr': {'novalidate': 'novalidate'}}) }}
        {{ form_widget(form) }}
    {{ form_end(form) }}
{% endmacro %}
