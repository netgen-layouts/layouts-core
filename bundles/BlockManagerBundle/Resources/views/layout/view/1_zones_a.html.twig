{% extends ngbm.pageLayout %}

{% block layout %}
<div class="zone-layout-1zonesa">

    {% if layout.zone('main').blocks is not empty %}
        <section class="zone zone-main">
            {% ngbm_render_zone layout.zone('main') %}
        </section>
    {% endif %}

</div>
{% endblock %}
