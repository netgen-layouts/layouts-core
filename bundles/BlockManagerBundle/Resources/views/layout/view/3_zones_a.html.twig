{% extends ngbm.configuration.parameter('pagelayout') %}

{% block layout %}
<div class="split">
    <div class="l">
        <div data-zone="top_left">
            {% include 'NetgenBlockManagerBundle:layout:blocks.html.twig' with {
                'blocks': layout.zones.top_left.blocks
            } %}
        </div>
    </div>

    <div class="r">
        <div data-zone="top_right">
            {% include 'NetgenBlockManagerBundle:layout:blocks.html.twig' with {
                'blocks': layout.zones.top_right.blocks
            } %}
        </div>
    </div>

    <div id="main" class="clearfix">
        {{ block('content') }}
    </div>

    <div class="r">
        <div data-zone="bottom">
            {% include 'NetgenBlockManagerBundle:layout:blocks.html.twig' with {
                'blocks': layout.zones.bottom.blocks
            } %}
        </div>
    </div>
</div>
{% endblock %}
