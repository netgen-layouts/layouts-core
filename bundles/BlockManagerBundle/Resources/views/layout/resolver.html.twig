{% extends ngbm.layoutView ? ngbm.layoutView.template : ngbm.pagelayout %}

{# We set some convenient variables to make it easier to write layout templates #}
{% if ngbm.layoutView %}
    {% set layout = ngbm.layoutView.layout %}
    {% set blocks = ngbm.layoutView.parameters.blocks %}
{% endif %}
