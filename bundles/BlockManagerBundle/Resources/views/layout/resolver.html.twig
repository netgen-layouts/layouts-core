{% extends ngbm.layoutTemplate ? ngbm.layoutTemplate : ngbm.pageLayout %}

{# We set some convenient variables to make it easier to write layout templates #}
{% if ngbm.layoutTemplate and ngbm.layout %}
    {% set layout = ngbm.layout %}
{% endif %}
