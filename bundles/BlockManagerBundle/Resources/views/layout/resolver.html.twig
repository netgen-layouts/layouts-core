{% extends ngbm.layoutView ? ngbm.layoutView.template : ngbm.configuration.parameter('pagelayout') %}

{# We set some convenient variables to make it easier to write layout templates #}
{% if ngbm.layoutView %}
    {% set layout = ngbm.layoutView.layout %}
{% endif %}
