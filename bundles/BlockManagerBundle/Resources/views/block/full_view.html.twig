{# This template is intentionally left blank #}

{# Block with "full_view" definition identifier cannot be rendered manually #}
{# so we define this template in order not to crash the page if someone tries #}
{# to render the block by hand #}
