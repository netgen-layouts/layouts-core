{% set form_action = path('netgen_block_manager_api_v1_edit_block', {'block_id': block.id}) %}

<p>Editing "{{ block.name }}" block</p>

{{ form_start(form, {'action': form_action, 'method': 'PATCH', 'attr': {'novalidate': 'novalidate'}}) }}
    {{ form_widget(form) }}

    <input type="submit" />
{{ form_end(form) }}
