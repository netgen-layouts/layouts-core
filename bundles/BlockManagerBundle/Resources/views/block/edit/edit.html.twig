{% set form_action = path('netgen_block_manager_api_v1_edit_block', { 'blockId': block.id }) %}

<p>Editing "{{ block.name }}" block</p>

<form action="{{ form_action }}" method="post" novalidate="novalidate">
    {{ form_widget(form) }}

    <input type="submit" />
</form>
