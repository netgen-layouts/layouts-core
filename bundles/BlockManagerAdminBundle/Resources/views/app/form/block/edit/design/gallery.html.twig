{% extends 'NetgenBlockManagerAdminBundle:app/form/block/edit:design.html.twig' %}

{% block parameters %}
    {% set parameters = form.children.parameters %}

    {{ form_row(parameters.children.next_and_previous, {attr: {'data-view-type': 'slider,thumb_gallery'}}) }}
    {{ form_row(parameters.children.show_pagination, {attr: {'data-view-type': 'slider'}}) }}
    {{ form_row(parameters.children.infinite_loop, {attr: {'data-view-type': 'slider,sushi_bar'}}) }}
    {{ form_row(parameters.children.transition, {attr: {'data-view-type': 'slider'}}) }}
    {{ form_row(parameters.children.autoplay, {attr: {'data-view-type': 'slider,sushi_bar'}}) }}
    {{ form_row(parameters.children.aspect_ratio, {attr: {'data-view-type': 'slider,thumb_gallery,grid_gallery,sushi_bar'}}) }}
    {{ form_row(parameters.children.show_details, {attr: {'data-view-type': 'thumb_gallery'}}) }}
    {{ form_row(parameters.children.number_of_thumbnails, {attr: {'data-view-type': 'grid_gallery,sushi_bar'}}) }}
    {{ form_row(parameters.children.enable_lightbox, {attr: {'data-view-type': 'grid_gallery'}}) }}

    {{ form_rest(parameters) }}
{% endblock %}
