{% extends '@NetgenLayoutsAdmin/app/form/modal.html.twig' %}

{% form_theme form '@NetgenLayoutsAdmin/form/theme/nglayouts.html.twig' %}
{% form_theme form.layoutType _self %}

{% trans_default_domain 'nglayouts_forms' %}

{% block _create_layoutType_widget %}
    <div {{ block('widget_container_attributes') }}>
    {%- for child in form %}
        <div class="layout-type-item">
            {{- form_widget(child) -}}
            {{- form_label(child) -}}
        </div>
    {% endfor -%}
    </div>
{% endblock %}

{% block _create_layoutType_entry_label %}
    <label for="{{ id }}" class="required">
        <i class="icon" {% if form.vars.layout_type.icon is not null %}style="background-image:url({{ asset(form.vars.layout_type.icon) }})"{% endif %}></i>
        {{ translation_domain is same as(false) ? label : label|trans({}, translation_domain) }}
    </label>
{% endblock %}

{% block children %}
    {{ form_row(form.layoutType, {attr: {class: 'layout-type'}}) }}
    {{ form_row(form.name) }}
    {{ form_row(form.description) }}
{% endblock %}

{% block buttons %}
    <button type="button" class="btn btn-link action_cancel">{{ 'layout.create.button.cancel'|trans }}</button>
    <button type="submit" class="btn btn-primary action_apply">{{ 'layout.create.button.create'|trans }}</button>
{% endblock %}
