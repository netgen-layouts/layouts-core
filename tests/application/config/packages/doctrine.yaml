parameters:
    env(DATABASE): ''
    env(DATABASE_CHARSET): ''

doctrine:
    dbal:
        connections:
            default:
                url: '%env(resolve:DATABASE)%'
                charset: '%env(resolve:DATABASE_CHARSET)%'
