block_definitions:
    list:
        view_types:
            test_grid:
                name: 'Test grid'
                item_view_types:
                    test_item:
                        name: 'Test item'
            other_grid:
                name: 'Other grid'
                item_view_types:
                    other_item:
                        name: 'Other item'

    list_without_value_types:
        name: 'List without value types'
        handler: list
        collections:
            default:
                valid_item_types: []
        view_types:
            list:
                name: 'List'

    list_without_query_types:
        name: 'List without query types'
        handler: list
        collections:
            default:
                valid_query_types: []
        view_types:
            list:
                name: 'List'
